function power(num1, num2) {
  return Math.pow(num1, num2);
}

function squareRoot(num) {
  return Math.sqrt(num);
}

function cubeRoot(num) {
  return Math.cbrt(num);
}

module.exports = { power, squareRoot, cubeRoot };
